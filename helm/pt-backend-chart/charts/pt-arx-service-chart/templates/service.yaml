apiVersion: v1
kind: Service
metadata:
  name: {{ include "pt-arx-service-chart.fullname" . }}
  labels:
    {{- include "pt-arx-service-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pt-arx-service-chart.selectorLabels" . | nindent 4 }}
