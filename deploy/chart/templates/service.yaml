apiVersion: v1
kind: Service
metadata:
  name: service_backend_{{ include "chart.nameid" . }}
  labels:
    version: {{ .Values.version }}
    nameid: {{ include "chart.nameid" . }}
spec:
  selector:
    app: service_backend_{{ include "chart.nameid" . }}
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: ClusterIP